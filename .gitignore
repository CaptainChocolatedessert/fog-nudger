# Dependencies
node_modules/

# Third-party source kept locally for reading only — see CLAUDE.md.
# Deliberately NOT committed: it is someone else's GPLv3 code and vendoring it into this
# repository would distribute it. Grepping a local clone is far cheaper than fetching files
# one at a time over the API, which is the only reason it is here.
reference/

# Build output
dist/
dist-ssr/
*.local

# Dev log shim output (see CLAUDE.md)
dev.log

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# Claude Code local settings and tooling
.claude/settings.local.json
.claude/launch.json

# Private operating notes for Claude — not project documentation.
# Project docs live in README.md and DESIGN.md.
CLAUDE.md

# A code review, internal like CLAUDE.md: a list of this repository's own defects, written to be
# acted on and thrown away rather than published. The 2026-08-30 one was worked through and deleted
# on 09-01, so nothing matches this today — the entry stays because the next one would otherwise be
# untracked but not ignored, which is one `git add -A` away from being neither.
REVIEW.md

# Scratch notes kept at the repository root. Anything belonging to the project goes in DESIGN.md.
W - tools brainstorming.txt

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
